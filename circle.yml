machine:
  node:
    version: 6.9.4

dependencies:
  pre:
    - npm i -g npm@latest
    - npm install -g cordova ionic

test:
  pre:
    - npm run lint
  override:
    - echo "Hello world!"

deployment:
  staging:
    branch: develop
    commands:
      - ionic build